name: Publish Packages

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      - name: Install dependencies
        run: bun install

      - name: Build all packages
        run: bun run build

      - name: Publish changed packages
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          for pkg in packages/*; do
            if [ -d "$pkg" ]; then
              echo "INFO: Attempting to publish package in '$pkg'"
              # Use the --tolerate-republish flag to prevent errors on existing versions
              (cd "$pkg" && bun publish --no-git-checks --tolerate-republish)
            fi
          done